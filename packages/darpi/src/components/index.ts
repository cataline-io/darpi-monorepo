import { schema, configure } from '@cataline.io/schema'

// components
export { default as Form } from './Form.vue'
export { default as Field } from './Field.vue'
export { default as Button } from './Button.vue'

// scripts
export { schema, configure }
